<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.codingblock.view">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MyViewActivity" />
        <activity android:name=".event_dispatch.EventDispatchActivity" />
        <activity android:name=".activity.StudyActivity" />
        <activity android:name=".activity.PaintStudyActivity" />
        <activity android:name=".game.DebrisShapeActivity" />
        <activity android:name=".conflict.ScrollListActivity"></activity>
    </application>

</manifest>